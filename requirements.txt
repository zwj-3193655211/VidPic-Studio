# Requirements for AI Image Generator (project venv: .venv)
# Install:  .venv/Scripts/pip install --index-url https://download.pytorch.org/whl/cu128 torch
#           .venv/Scripts/pip install -r requirements.txt
#
# NOTE: transformers MUST stay <5 (diffusers from_single_file / SDXL ckpt loading
#       is incompatible with transformers 5.x CLIP refactor).

# Core dependencies
gradio>=4.0.0
Pillow>=10.0.0
numpy>=1.24.0

# torch (CUDA build installed separately from download.pytorch.org/whl/cu128)
torch>=2.0.0

# Diffusers + transformers (pinned <5 for SDXL single-file support)
diffusers>=0.30.0
transformers>=4.41.0,<5
accelerate>=0.25.0
safetensors>=0.4.0

# Development dependencies
pytest>=7.4.0

# Video generation (LTX-Video) + export
imageio>=2.30.0
imageio-ffmpeg>=0.4.9
sentencepiece>=0.1.99
protobuf>=3.20.0
